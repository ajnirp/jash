Todo
----

C-z stops a process

aliases need to be expanded properly. while parsing the bash aliases file, expand a new alias in terms of the ones seen upto that point, just like bash itself does.

tab completion (might need to search the PATH variable)

case insensitive tab completion -> .rc maybe?